#include <stdio.h>
#include <string.h>

int main () {
    char string [15];
    int leng, g;
    printf("Enter a word: ");
    scanf("%s", string);
    leng = strlen(string);
    int i = 0;
    for()
    printf("Area of a triangle = %f\n", area);
    return 0;
}